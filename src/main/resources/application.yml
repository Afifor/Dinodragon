#Global configuration
spring:

  application:
    name: dinodragon
    #H2 Configuration
    h2.console.enabled: true

  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dinodragon
    username: root
    password: pouledeau

  jpa:
    hibernate.ddl-auto: update
    show-sql: true

server:
  servlet:
    context-path: /dinodragon

#Log level configuration
logging:
  level:
    error: DEBUG

